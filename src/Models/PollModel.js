class PollModel {

    constructor({ id, title, items, type }) {
        this.id = id
        this.title = title;
        this.items = items;
        this.type = type;
    }
}

export default PollModel;